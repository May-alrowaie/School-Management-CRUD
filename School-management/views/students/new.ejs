<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Student Record</title>
  </head>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-image: url("https://img.freepik.com/premium-vector/hand-drawn-dark-background-back-school-with-portfolio-watches-school-supplies_638892-196.jpg?w=826");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      color: #f5f5f5; /* Light gray text for good readability */
    }

    /* Container styling */
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      background-color: rgba(
        20,
        20,
        20,
        0.85
      ); /* Darker semi-transparent black */
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
    }

    /* Navigation styles */
    .nav {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-bottom: 20px;
    }

    a {
      text-decoration: none;
      color: #80cbc4; /* Soft teal for links */
      font-size: 1.1em;
      padding: 10px 20px;
      border-radius: 5px;
      transition: background-color 0.3s, color 0.3s;
    }

    a.button {
      background-color: #1f1f1f; /* Dark button background */
      border: 1px solid #333;
    }

    a.button:hover {
      background-color: #4db6ac; /* Brighter teal for hover */
      color: #ffffff; /* White text on hover */
      border: 1px solid #555;
    }

    /* Header styles */
    header {
      margin-bottom: 20px;
    }

    h1,
    h2 {
      font-size: 2.5em;
      color: #ffffff; /* Pure white for strong headers */
      margin-bottom: 10px;
      font-weight: bold;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7); /* Subtle shadow for depth */
    }

    p,
    label {
      font-size: 1.2em;
      color: #cccccc; /* Lighter gray for subtext */
      margin: 10px 0;
    }

    /* Button styles */
    button {
      background-color: #1f1f1f; /* Dark button background */
      color: #f5f5f5; /* Light gray text */
      font-size: 1.1em;
      padding: 12px 25px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.3s;
    }

    button:hover {
      background-color: #4db6ac; /* Teal hover color */
      color: #ffffff; /* Bright white text */
      transform: scale(1.05);
    }

    /* Input fields */
    input[type="text"],
    input[type="number"],
    select,
    input[type="file"] {
      font-size: 1em;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #444;
      border-radius: 5px;
      background-color: #1f1f1f;
      color: #f5f5f5; /* Light text for readability */
      width: 100%;
    }

    input[type="checkbox"] {
      transform: scale(1.5);
      margin-right: 10px;
      margin-bottom: 20px;
    }

    /* Student List Page */
    .student-list {
      display: grid;
      grid-template-columns: repeat(
        auto-fill,
        minmax(200px, 1fr)
      ); /* Responsive grid */
      gap: 20px;
      padding: 20px;
    }

    .student-list .student-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 15px;
      background-color: #1f1f1f;
      border: 1px solid #333;
      border-radius: 8px;
      color: #f5f5f5; /* Light text */
      text-decoration: none;
      transition: background-color 0.3s, border-color 0.3s;
    }

    .student-list .student-item:hover {
      background-color: #3d3d3d; /* Slightly lighter background */
      border-color: #555;
    }

    @media (max-width: 600px) {
      .nav {
        flex-direction: column;
        gap: 10px;
      }

      a {
        padding: 10px 15px;
      }

      h1,
      h2 {
        font-size: 2em;
      }

      p,
      label {
        font-size: 1em;
      }

      input[type="text"],
      input[type="number"],
      select {
        font-size: 1em;
        padding: 10px;
      }
      form {
        max-width: 500px;
        margin: 0 auto; /* Center the form on the page */
        padding: 20px;
        background-color: #ffffff; /* White background for the form */
        border-radius: 10px; /* Rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for better contrast */
        font-family: Arial, sans-serif;
      }

      label {
        font-size: 1.2em;
        font-weight: bold;
        color: #010101; /* Dark gray for text to ensure readability */
        margin-bottom: 5px;
        display: block; /* Ensures each label is on its own line */
      }

      input[type="text"],
      input[type="number"] {
        font-size: 1em;
        padding: 10px;
        margin-bottom: 15px;
        border: 1px solid #cccccc; /* Light gray border */
        border-radius: 5px;
        background-color: #f9f9f9; /* Slightly off-white background for inputs */
        color: #333333; /* Dark text for readability */
        width: 100%; /* Make inputs full-width */
        box-sizing: border-box; /* Ensures padding doesn't overflow */
      }

      input[type="text"]:focus,
      input[type="number"]:focus {
        border-color: #80cbc4; /* Teal border on focus */
        outline: none; /* Remove default blue outline */
        background-color: #ffffff; /* Pure white background on focus */
      }

      button {
        background-color: #4caf50; /* Green button background */
        color: #ffffff; /* White text for contrast */
        font-size: 1.2em;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s, transform 0.3s;
      }

      button:hover {
        background-color: #45a049; /* Slightly darker green on hover */
        transform: scale(1.05); /* Subtle zoom effect */
      }

      br {
        margin-bottom: 15px; /* Add spacing between rows */
      }
    }
  </style>
  <body>
    <%- include('../partials/_navbar.ejs')%>
    <br />
    <br />
    <br />

    <h1>Add a New Student Record</h1>
    <form method="post" action="/students">
      <label for="school">School:</label>
      <input type="text" name="school" id="school" />
      <br />
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" />
      <br />
      <label for="phone">Phone Number:</label>
      <input type="number" name="phone" id="phone" min="0" />
      <br />
      <label for="age">Age:</label>
      <input type="number" name="age" id="age" min="0" />
      <br />
      <button type="submit">Add Student Record</button>
    </form>
  </body>
</html>
